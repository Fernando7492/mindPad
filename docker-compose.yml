version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/target:/app/target 
    restart: always

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    restart: always
